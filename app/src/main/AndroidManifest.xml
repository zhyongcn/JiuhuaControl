<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jiuhua.jiuhuacontrol">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".room.RoomActivity2"></activity>
        <activity
            android:name=".MainActivity1"
            android:label="周先生雅居">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".RepairActivity" />
        <activity android:name=".UserinfoActivity" />
        <activity android:name=".DocumentsAndRepairActivity" />
        <activity android:name=".timerSettingActivity" />
        <activity android:name=".OutdoorActivity" />
        <activity android:name=".room.RoomActivity" />
    </application>

</manifest>